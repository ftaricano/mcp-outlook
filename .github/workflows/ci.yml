name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    name: Lint + Typecheck + Test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      matrix:
        node: [20, 22, 24]

    steps:
      - uses: actions/checkout@v7

      - name: Setup Node ${{ matrix.node }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test -- --reporter=default

      - name: Smoke test (MCP protocol)
        run: npm run smoke

      - name: Coverage
        if: matrix.node == '20'
        run: npm run test:coverage

      - name: Upload coverage
        if: matrix.node == '20'
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: coverage/
          retention-days: 14

  audit:
    name: Dependency audit (prod only)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm
      # --ignore-scripts prevents the `prepare` hook (which runs tsc) from
      # executing without devDependencies available, avoiding a false failure.
      - run: npm ci --omit=dev --ignore-scripts
      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high
